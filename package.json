{
  "name": "vibe-coding-remote",
  "private": true,
  "packageManager": "pnpm@10.11.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "pnpm --filter @vibe-coding-remote/web run dev -- --host",
    "dev:desktop": "pnpm --filter @vibe-coding-remote/desktop tauri dev",
    "typecheck": "pnpm -w exec tsc -p packages/app/tsconfig.json --noEmit && pnpm --filter @vibe-coding-remote/web run typecheck && pnpm --filter @vibe-coding-remote/desktop run typecheck",
    "lint:frontend": "pnpm exec biome check apps packages package.json",
    "format:frontend": "pnpm exec biome check --write apps packages package.json",
    "build:web": "pnpm --filter @vibe-coding-remote/web run build",
    "build:desktop": "pnpm --filter @vibe-coding-remote/desktop tauri build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "typescript": "~6.0.2"
  }
}
